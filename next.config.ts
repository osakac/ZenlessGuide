import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Портреты агентов — 374×512, самый крупный показ — ~280px на странице
    // агента. Стандартные наборы давали srcset из девяти ширин до 3840w:
    // всё от 384 и выше — та же картинка 374px, но лишние сотни байт на каждый
    // <img> и <link rel="preload"> и отдельная копия в кэше оптимизатора.
    // 128/256 — плитки тир-листа и составов (~120px на 1x/2x), 384 — карточки,
    // 640 — запас на случай портретов крупнее нынешних.
    deviceSizes: [640],
    imageSizes: [128, 256, 384],
  },
  experimental: {
    // `radix-ui` — монопакет, реэкспортирующий все примитивы. В список
    // оптимизируемых по умолчанию он не входит (в отличие от lucide-react),
    // а без этого сборщик разбирает весь barrel ради пяти примитивов.
    optimizePackageImports: ["radix-ui"],
  },
};

export default nextConfig;
